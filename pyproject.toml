[project]
name = "wecom-issue-radar-worker"
version = "2.1.0"
requires-python = ">=3.11"
dependencies = [
    "pycryptodome>=3.18",
    "openpyxl>=3.1",
    "tzdata>=2025.2",
    "mcp>=1.13,<2",
]

[project.optional-dependencies]
worker-build = [
    "pyinstaller>=6.0",
]
